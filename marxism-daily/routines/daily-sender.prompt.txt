[Daily Marxism daily send — 09:00 UTC firing] This is a FRESH session with no memory of earlier runs. Everything you need is in the repo adeoo/pipedream (cloned at /home/user/pipedream; clone it if missing) on branch claude/daily-programs. Do not use any other branch. Your job: deliver today's Daily Marxism email via Inkbox: a lesson from Monday to Saturday, the quiz on Sunday. You are sending real email to real people. Follow the SEND PROTOCOL in step 5 to the letter; it exists because earlier runs sent broken and duplicated copies.

1. git fetch origin claude/daily-programs and check out / fast-forward to it.
2. Compute today's date and weekday in America/Sao_Paulo: TZ=America/Sao_Paulo date "+%F %A".
3. MONDAY TO SATURDAY, lesson day: find the lesson file marxism-daily/lessons/weekNN/dayNN.md whose front-matter send_date equals today. If no such file exists, stop and report clearly that no lesson exists for today (the Thursday writer Routine "Daily Marxism — weekly lesson writer (fresh session)" likely failed). Do not write a lesson yourself.
   Idempotency: call inkbox_emails_list with direction "outbound" and q = the lesson's English subject, then again with the Portuguese subject. A delivered email with that exact subject sent today means that language is done. Send only whichever language is missing; if both went out, stop with a no-op note.
   Render: python3 marxism-daily/render.py <file> --write /tmp/send. This writes /tmp/send/en.subject.txt, en.html, en.txt, pt.subject.txt, pt.html, pt.txt and prints one check line per file (size, first and last characters).
   Then send two emails from the adeosagent identity mailbox adeosagent@inkboxmail.com, one at a time, each by the SEND PROTOCOL: the English email to moussaadel97@gmail.com from the en.* files, then the PT-BR email to ana.ruberrime@gmail.com from the pt.* files.
4. SUNDAY, quiz day: do not send a lesson. If a lesson file carries today's date, mention that as an error in your final status and skip it. Find the file marxism-daily/quizzes/quizNN.json whose send_date equals today. It gives the quiz number (quiz), the covered days (covers_days), the covered dates (covers_dates, covers_dates_pt), the subjects (subject, subject_pt) and the file names (file = the English HTML, file_pt = the PT-BR HTML, both in marxism-daily/quizzes/). If no such file exists, stop and report clearly that no quiz exists for today. Idempotency as in step 3, with the quiz subjects. Attach each quiz file like this: base64-encode it (base64 -w0 <file>), call inkbox_email_attachment_upload with the filename and source {kind: "base64", data: <base64>, content_type: "text/html"}, then pass the returned media_handle, content_hash, filename and content_type in the attachments list of inkbox_email_send. Send two emails from adeosagent@inkboxmail.com: the English email to moussaadel97@gmail.com with subject and the English HTML file ATTACHED, and the PT-BR email to ana.ruberrime@gmail.com with subject_pt and the PT-BR HTML file ATTACHED. The quiz HTML goes only as an attachment, never in the email body: mail clients remove scripts and the interactive parts die. The body is plain text: body_text = the text below; body_html = the same text with each paragraph wrapped in <p> and </p>. Fill in the values in braces from the json. After each send, verify with inkbox_email_get (max_body_chars 300): has_attachments must be true and body_text must begin with "Good morning" (EN) or "Bom dia" (PT). On failure apply step 5d.

English body:
Good morning, Moussa.

Today there is no lesson. There is a quiz. It covers the lessons from {covers_dates} (days {covers_days}).

The quiz is the attached file, {file}. Save the file, then open it in a browser (Chrome, Safari or Firefox). It works on a phone too. It does not work inside the email.

The quiz has three parts: a classification drill, an error hunt, and writing practice with model answers. There are no grades and no timers. Take about 30 minutes.

Normal lessons return on Monday.

Quiz {quiz}. Daily Marxism.

PT-BR body:
Bom dia, Carol.

Hoje não tem aula. Tem uma prova. Ela cobre as aulas de {covers_dates_pt} (dias {covers_days}).

A prova é o arquivo anexo, {file_pt}. Salve o arquivo e abra em um navegador (Chrome, Safari ou Firefox). Funciona no celular também. Dentro do e-mail a prova não funciona.

A prova tem três partes: um exercício de classificação, uma caça ao erro e uma prática de escrita com respostas modelo. Não tem nota nem cronômetro. Reserve uns 30 minutos.

As aulas normais voltam na segunda-feira.

Prova {quiz}. Daily Marxism.

If the attachment upload or the send with attachments fails after one retry, do not paste the HTML into the body. Send the body above with one extra line at the end: "The quiz file could not be attached. It is in the repo adeoo/pipedream, branch claude/daily-programs, at marxism-daily/quizzes/{file}." (PT: "Não foi possível anexar o arquivo da prova. Ele está no repositório adeoo/pipedream, branch claude/daily-programs, em marxism-daily/quizzes/{file_pt}."). Then name the exact limitation in your final status line.
5. SEND PROTOCOL, one email at a time:
   a. Display the three files with Bash: cat /tmp/send/en.subject.txt, then cat /tmp/send/en.html (one long line; that is expected), then cat /tmp/send/en.txt. Same with the pt.* files for the Portuguese email.
   b. Call inkbox_email_send once with recipients {"to": ["<address>"]}, subject = the content of the .subject.txt file, body_html = the content of the .html file, body_text = the content of the .txt file. Copy each file's content exactly as Bash displayed it, from its first character to its last. The tool takes raw HTML and raw text: do not turn < into &lt; or " into &quot;, do not add backslashes or \n, do not wrap anything in quotes or JSON, do not shorten, summarize, reformat or "fix" the content, do not put a shell command or a $(...) expression in any field (tool parameters are not a shell), and never send a test, sample or placeholder email to anybody.
   c. Verify: take the id the send returned and call inkbox_email_get with that message_id and max_body_chars 300. PASS when body_html begins with "<div style=" and body_text begins with the subject line. FAIL when body_html begins with "&lt;", is null, or contains "placeholder", or when body_text contains "$(" or does not begin with the subject line.
   d. On FAIL, send exactly one corrected email: same recipient, subject = "Corrected copy: " + subject (PT-BR: "Cópia corrigida: " + subject), body_html and body_text copied again from the same files, then verify it the same way. Never send a third copy. Name the failure in your final status line.
   e. If inkbox_email_send itself returns an error, retry it once, then report.

Do not modify the repo, do not write lessons or quizzes, do not change any Routine. End your reply with ONE status line: "OK: <subject> sent (EN + PT), verified" if everything worked, or "FAILED: <exact error and which step>" if anything failed. That final line is what reaches Moussa as a push/email notification, so keep it clear and short.
