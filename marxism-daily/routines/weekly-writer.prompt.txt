[Daily Marxism weekly writer — scheduled Thursday firing] This is a FRESH session with no memory of earlier runs. The single source of truth is the marxism-daily/ directory in repo adeoo/pipedream (cloned at /home/user/pipedream; clone it if missing) on branch claude/daily-programs. Do not read from or push to any other branch. Your job: write the next batch, which is normally the Sunday quiz plus six lessons.

1. git fetch origin claude/daily-programs and check out / fast-forward to it.
2. Read IN FULL: marxism-daily/PROGRAM.md (v2, especially the sections "Sunday quiz" and "Weekly batch and calendar"), marxism-daily/state.json, .claude/skills/test-workbook/SKILL.md, and every lesson of the most recent week (completely, not skimmed: they give continuity and they are the source material of the quiz). Follow PROGRAM.md exactly: the voice rules, the 4 theory / 1 history / 1 region rhythm (there is no review lesson anymore; the Sunday quiz replaced it), and the bilingual lesson file format (subject, subject_pt and send_date front matter; English lesson, then === PT-BR ===, then a natural Portuguese version).
3. If state.json has a "transition" field, write exactly what that field describes and nothing else. Otherwise write the normal batch: (a) the quiz for the coming Sunday: number = next_quiz, send_date = next_quiz_send_date, covering days next_quiz_covers_days (these lessons are already written; never test a lesson that is not written); (b) the six lessons of week next_week: days next_day to next_day+5, send_dates from Monday next_batch_first_send_date to Saturday, as marxism-daily/lessons/weekNN/dayNN.md with global day numbering. No lesson may carry a Sunday send_date. Before writing, check which of these files already exist (a previous run may have stopped halfway) and continue from there instead of rewriting finished files.
4. Build the quiz with the test-workbook skill. Reference example: marxism-daily/quizzes/REFERENCE-week1-workbook.html; copy its page structure, CSS and JavaScript, replace only the content. Three parts: classification drill, find the error, written analysis with model answers. Transfer, not recall: new cases, no quotes from the lessons, no reuse of their examples. Plausible distractors, feedback that teaches, honest grey zones, no grades, no timers, a "try again" button on every drill. Prose in ASD-STE100 style: short sentences, active voice, plain words, no em dashes, no emojis. Write three files in marxism-daily/quizzes/: quizNN.html (English, for Moussa), quizNN.pt.html (a natural PT-BR rewrite for Carol, complete and standalone, same structure), and quizNN.json with the fields quiz, send_date, covers_days, covers_dates, covers_dates_pt, subject, subject_pt, file, file_pt (format and example in PROGRAM.md). Keep the stylesheet link ../../assets/course.css plus the inline mirror of the CSS, so each file also works alone as an email attachment. Footer inside the page: "Quiz N, covering days X to Y. Daily Marxism." (PT: "Prova N, dias X a Y. Daily Marxism."). Subject lines obey the lesson rules: intriguing and specific, no emoji, no dashes, no "Quiz N" prefix.
5. Checks before you commit: python3 marxism-daily/render.py <file> must output subject, html, text, subject_pt, html_pt and text_pt without error for every lesson. quizNN.json must be valid JSON. In both quiz HTML files, every block with data-q must contain exactly one data-correct option, no correct answer may be the longest option of its item, and the file must contain the full <script> block from the reference.
6. Do NOT send or schedule any email. Delivery is handled by the Routine "Daily Marxism — daily send via Inkbox (06:00 Brasília, fresh session)": a lesson Monday to Saturday, the quiz as an HTML attachment on Sunday. English to moussaadel97@gmail.com, PT-BR to ana.ruberrime@gmail.com.
7. Update state.json exactly as PROGRAM.md and the transition field describe: next_day = the day after the last lesson written, next_week += 1, next_batch_first_send_date = the Monday after the last lesson written, next_quiz += 1, next_quiz_send_date = the Sunday after the last lesson written, next_quiz_covers_days = the day range of the lessons you just wrote, last_batch_written = today. Delete the transition field once you completed it. Commit, then git push -u origin claude/daily-programs (retry with backoff on network errors). No pull request.
8. When next_week is greater than 12 (all weeks are written), write only the final Sunday quiz plus a short closing recap as one extra lesson file dated the Monday after that quiz, update state.json, push, then disable this Routine: use list_triggers to find the Routine named "Daily Marxism — weekly lesson writer (fresh session)" and call update_trigger with enabled=false.

End your reply with ONE status line: "OK: quiz N + days X–Y written, pushed" if everything worked, or "FAILED: <exact error and which step>" if anything failed. That final line is what reaches Moussa as a push/email notification, so keep it clear and short.
